spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop # Or update if you prefer
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  h2:
    console:
      enabled: true # If you want to enable the H2 console
# Additional configurations for the email service in the test environment
  mail:
    username: marcosondruska@gmail.com
    password: eweq xthr owsm yrmw

